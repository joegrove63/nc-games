import React, { useEffect } from 'react';
import * as api from '../utils/api';

const Home = ({ reviews, setReviews }) => {
  // useEffect(() => {
  //   api.fetchReviews;
  // });
  return <div></div>;
};

export default Home;
